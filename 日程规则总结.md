# 运动会日程安排规则总结

## 系统架构说明

日程生成采用**「确定性规则引擎 + AI 智能优化」**双层架构：

1. **服务器端（`schedule_gen.py`）**：确定性地完成分组分道，生成日程骨架
2. **AI 端（`schedule_ai.py` + DeepSeek API）**：仅负责调整时间、顺序、时段，不改变分组

---

## 一、服务器端规则（确定性生成）

### 文件位置
- `backend/app/services/schedule_gen.py` - 核心生成逻辑
- `backend/app/services/schedule_rules.py` - 规则文本定义

### 1.1 赛段自动分配

**规则**：根据比赛天数和年级，自动分配到不同时段

```python
# 2天赛程
小学（一~六年级）: 第1天上午 + 第1天下午
初高中（初一~高三）: 第2天上午 + 第2天下午

# 3天赛程
小学: 第1天全天 + 第2天上午（1.5天）
初高中: 第2天下午 + 第3天全天（1.5天）
```

**实现函数**：`half_day_slots(days: int)`

**详细说明**：

#### 设计背景
- **年龄分层**：小学生（6-12岁）与初高中生（13-18岁）的体能差异大，分开举办可确保：
  - 项目难度适配（小学无1500米、标枪等高强度项目）
  - 场地器材调整时间（跳高架高度、跑道起跑器位置）
  - 裁判和志愿者专注服务一个年龄段
  
- **时间均衡**：
  - 2天赛程：各占1天（4个半天槽位，每段2个）
  - 3天赛程：各占1.5天（6个半天槽位，每段3个）
  - 确保两个赛段的比赛密度相当

#### 实现细节

```python
def half_day_slots(days: int) -> dict[str, list[tuple[int, str]]]:
    """返回 {赛段名: [(天数, 时段), ...]}"""
    if days >= 3:
        return {
            "小学": [(1, "上午"), (1, "下午"), (2, "上午")],
            "初高中": [(2, "下午"), (3, "上午"), (3, "下午")],
        }
    # days == 2 的情况
    return {
        "小学": [(1, "上午"), (1, "下午")],
        "初高中": [(2, "上午"), (2, "下午")],
    }
```

**关键点**：
1. **槽位结构**：每个半天是一个独立槽位，用 `(day_index, period)` 元组表示
2. **槽位分配**：项目按优先级排序后，均匀分配到赛段的各个槽位
3. **跨天隔离**：小学和初高中在3天赛程中有半天交接（第2天），但不会同时占用同一槽位

#### 边界情况
- **天数 < 2**：理论上不支持，实际系统最小2天
- **天数 > 3**：统一按3天处理（代码中 `if days >= 3`）
- **混合年级项目**：如"小学vs初中接力"，需手动归类到某个赛段

#### 与其他规则的交互
- **与规则1.4（分组）**：先确定赛段槽位，再在槽位内分组
- **与规则1.6（预决赛间隔）**：预决赛至少隔半天，在同一赛段内才有意义
- **与规则1.8（运动员冲突）**：同一赛段内的运动员可能兼项，需要冲突检测

#### 实际运行表现
- **2天赛程**：220条赛次，小学约110条（第1天），初高中约110条（第2天）
- **3天赛程**：小学项目较多时，第2天上午可能排得很满
- **优化空间**：AI可以在保持赛段划分的前提下，微调某些项目的具体时间

---

### 1.2 项目排序优先级

**排序键**：`(优先级, 年级规范序, 项目名称, 性别, 类型)`

**高负荷项目优先级**（数字越小越优先）：
```python
PRIORITY_EVENTS = {
    "100米": 1,
    "200米": 1,
    "60米": 1,
    "跳远": 1,
    "跳高": 1,
    "三级跳": 1,
    "400米": 2,
    "800米": 3,
    "1500米": 4,
}
# 其他项目：99（最低优先级）
```

**关键特性**：
- 同项目名称的男女连续完成（通过 `e.gender` 作为排序键实现）
- 短跑、跳跃等高负荷项目优先安排

**详细说明**：

#### 设计背景
- **体能恢复考虑**：高负荷项目（爆发力项目）对运动员消耗大，优先安排可以：
  - 确保运动员在体能最佳状态下比赛
  - 为后续可能的兼项留出充足恢复时间
  - 避免下午或赛程末尾才比高强度项目，导致成绩不佳

- **场地准备效率**：
  - 短跑项目场地准备简单（清理跑道、摆起跑器）
  - 跳跃项目需要沙坑平整、测量工具就位
  - 优先完成这些项目后，可以空出场地供其他项目使用

- **观赏性安排**：
  - 短跑、跳跃是运动会最具观赏性的项目
  - 安排在上午或赛程前段，观众精力充沛

#### 实现细节

```python
def event_priority(ev: Event) -> int:
    """返回项目优先级，数字越小越优先"""
    return PRIORITY_EVENTS.get(ev.name, 99)

# 在 build_skeleton 中的排序
events.sort(
    key=lambda e: (
        event_priority(e),      # 1. 优先级（1/2/3/4/99）
        _grade_key(e.group_name),  # 2. 年级序（一年级=1, 二年级=2...）
        e.name,                 # 3. 项目名称（字典序）
        e.gender,               # 4. 性别（男/女/混合）
        e.is_team               # 5. 类型（个人False=0, 团队True=1）
    )
)
```

**排序效果示例**：
```
优先级1（最优先）：
  - 三年级男100米
  - 三年级女100米  ← 同项目男女连续
  - 四年级男100米
  - 四年级女100米
  - 三年级男跳远
  - 三年级女跳远

优先级2：
  - 初一男400米
  - 初一女400米

优先级3：
  - 初二男800米
  - 初二女800米

优先级99（最低）：
  - 二年级男袋鼠跳
  - 二年级女袋鼠跳
  - 五年级播种与收割（团队）
```

#### 关键设计点

**1. 性别连续性**（规则2的实现）
- 排序键中 `e.gender` 确保同项目的男女赛次相邻
- 原因：
  - 裁判无需切换项目规则，只需等待下一组
  - 场地器材无需调整（如跳高架高度相同）
  - 广播员可以连续播报，减少混乱

**2. 年级内聚性**
- 同一项目不同年级按年级顺序排列
- 好处：同一学段的学生可以连续观看比赛，增强氛围

**3. 个人优先团队**
- `e.is_team` 作为最后排序键，个人项目（False=0）排在团队项目（True=1）前面
- 原因：个人项目人数多、赛次多，需要更多时间；团队项目可以穿插或放在末尾

#### 优先级配置原则

**Level 1（优先级=1）**：纯爆发力项目
- 短跑：60米、100米、200米（需要极短时间的最大功率输出）
- 跳跃：跳远、跳高、三级跳（单次爆发，对疲劳极敏感）

**Level 2（优先级=2）**：混合能量系统
- 400米（无氧+有氧混合，需要速度耐力）

**Level 3（优先级=3）**：耐力为主
- 800米（有氧为主，乳酸耐受）

**Level 4（优先级=4）**：纯耐力
- 1500米（有氧系统主导，恢复需要更长时间）

**Level 99（默认）**：其他项目
- 投掷类：垒球、实心球（技术性强，对疲劳不敏感）
- 趣味项目：袋鼠跳、播种与收割（竞技性弱，娱乐性强）
- 团队接力：100×4米（通常作为压轴项目）

#### 边界情况

**1. 同优先级项目很多**
- 示例：60米、100米、200米、跳远、跳高都是优先级1
- 解决：按年级→项目名→性别细分，确保稳定顺序

**2. 新增项目未配置优先级**
- 默认 99（最低），排在所有已配置项目之后
- 需要手动添加到 `PRIORITY_EVENTS` 字典

**3. 特殊项目需求**
- 示例：师生同乐（娱乐性项目，用户希望放在下午缓解疲劳）
- 解决：AI 优化阶段可以调整，或手动编辑

#### 与其他规则的交互

- **与规则1.1（赛段分配）**：先按赛段划分，再在赛段内按优先级排序
- **与规则1.5（项目组间隔）**：优先级高的项目通常间隔短（如100米3分钟），可以快速完成
- **与规则1.8（运动员冲突）**：高优先级项目优先占位，低优先级项目在冲突时更容易被调整

#### 实际运行表现
- **测试数据**：220条赛次，前50条几乎全是短跑和跳跃
- **AI优化**：用户可以通过自然语言调整，如"把跳远移到下午"
- **灵活性**：规则生成提供合理默认，AI优化提供个性化调整

---

### 1.3 预赛决赛判断

**规则**：
```python
报名人数 <= 决赛队伍数：直接决赛
报名人数 > 决赛队伍数 且 > 跑道数：预赛 + 决赛
```

**决赛队伍数来源**：项目定义中的 `final_teams` 字段

**实现位置**：`build_skeleton()` 函数

**详细说明**：

#### 设计背景
- **竞技公平性**：预赛筛选，确保决赛是最强者的对决
- **时间效率**：报名人数少时直接决赛，避免不必要的预赛环节
- **场地容量**：跑道数限制了单次可比赛人数，超出需要分组

#### 实现细节

```python
def build_skeleton(db: Session, year_id: int, config: ScheduleConfig):
    for ev in events:
        parts = _reg_counts(db, ev)  # 获取报名者列表
        if not parts:
            continue  # 无报名者不排赛次
            
        count = len(parts)  # 报名人数
        threshold = ev.final_teams if ev.final_teams > 0 else lanes
        # ↑ 决赛队伍数，若项目未设置则默认用跑道数
        
        if count > threshold and count > lanes:
            # 情况1：报名人数超过决赛队伍数，且超过跑道数
            # → 需要预赛筛选 + 决赛
            prelim = ScheduleEntry(
                round_type="预赛",
                advance_count=threshold,  # 取前N名进决赛
            )
            _fill_groups(prelim, parts, ev.is_team, lanes, db)
            
            final = ScheduleEntry(
                round_type="决赛",
                advance_count=threshold,
                group_count=len(split_into_groups(threshold, lanes)) or 1,
            )
            # 决赛不预先填充人员，由预赛成绩产生
            
        else:
            # 情况2：报名人数 <= 决赛队伍数，或 <= 跑道数
            # → 直接决赛
            final = ScheduleEntry(
                round_type="决赛",
                advance_count=0,  # 0 表示非筛选性决赛
            )
            _fill_groups(final, parts, ev.is_team, lanes, db)
```

#### 判断逻辑详解

**阈值计算**：
```python
threshold = ev.final_teams if ev.final_teams > 0 else lanes
```
- `ev.final_teams`：项目定义中的决赛队伍数（如100米取前8名）
- 若未设置（0或NULL），默认用跑道数（通常6-8道）
- 这个阈值是"预赛取几名进决赛"的依据

**条件1：`count > threshold`**
- 报名人数超过决赛容量
- 示例：100米报名30人，决赛取8名 → 需要预赛筛选

**条件2：`count > lanes`**
- 报名人数超过跑道数，单次无法全部比赛
- 示例：跑道6道，报名15人 → 需要分多组

**两个条件同时满足**：预赛 + 决赛
**任一条件不满足**：直接决赛

#### 各种情况举例

| 报名人数 | 决赛队伍数 | 跑道数 | 判断结果 | 说明 |
|---------|----------|--------|---------|------|
| 30 | 8 | 6 | 预赛+决赛 | 30>8 且 30>6，需预赛筛选 |
| 6 | 8 | 6 | 直接决赛 | 6≤8，人数未超决赛容量 |
| 10 | 8 | 6 | 预赛+决赛 | 10>8 且 10>6，需预赛筛选 |
| 8 | 8 | 6 | 预赛+决赛 | 8=8 但 8>6，需分组但不筛选（特殊） |
| 5 | 8 | 6 | 直接决赛 | 5≤8 且 5<6，单组决赛 |
| 12 | 20 | 6 | 直接决赛 | 12<20，虽然12>6但不需筛选，多组决赛 |

**特殊情况**：
- **报名人数 = 决赛队伍数**：如8人报名，决赛取8名
  - 若8 > 跑道数（如6道），仍生成预赛+决赛（分组但不淘汰）
  - 若8 ≤ 跑道数，直接决赛
  
- **决赛队伍数未设置**：使用跑道数作为阈值
  - 示例：跑道6道，报名10人，threshold=6
  - 10>6 且 10>6 → 预赛+决赛

#### 预赛取名逻辑

**`advance_count` 字段**：
```python
prelim.advance_count = threshold  # 预赛取前N名进决赛
```

**决赛名单生成**（见 `schedule_finals.py`）：
1. 用户录入预赛成绩
2. 系统按成绩排序，取前 `advance_count` 名
3. 自动创建决赛赛次，填充决赛名单

**示例**：
```
项目：三年级男100米
报名：30人
决赛队伍数：8人
跑道数：6道

预赛：
  - 第1组：6人（第1道~第6道）
  - 第2组：6人
  - 第3组：6人
  - 第4组：6人
  - 第5组：6人（最后一组可能不满）
  
预赛录入成绩后，取全部30人中成绩最好的前8名

决赛：
  - 第1组：8人（可能分为2组，每组4人，或1组8人取决于跑道数）
```

#### 团队项目的特殊处理

团队项目（如接力）通常：
- 报名以班级为单位
- 决赛队伍数较少（如4-6队）
- 逻辑相同，但 `parts` 是 `ClassTeam` 列表而非 `Athlete` 列表

**示例**：
```
项目：四年级100×4米接力
报名：12个班级
决赛队伍数：6队
跑道数：6道

判断：12>6 且 12>6 → 预赛+决赛
预赛：12队分2组，每组6队
决赛：取成绩最好的6队
```

#### 边界情况与异常处理

**1. 无报名者**
```python
if not parts:
    continue  # 不生成任何赛次
```

**2. 决赛队伍数 > 报名人数**
- 直接决赛，所有人参加
- `advance_count=0` 表示非筛选性决赛

**3. 决赛队伍数 = 0 或未设置**
- 使用跑道数作为默认阈值
- 确保逻辑不会因配置缺失而崩溃

**4. 报名人数 = 跑道数 + 1**
- 示例：7人报名，6道跑道，决赛取6名
- 判断：7>6 且 7>6 → 预赛+决赛
- 预赛分2组（6人+1人），取前6名
- 实际上1人必然被淘汰

#### 与其他规则的交互

- **与规则1.4（分组）**：预赛分组用 `split_into_groups`，决赛分组由预赛成绩决定
- **与规则1.6（预决赛间隔）**：只有生成了预赛+决赛，才需要检查间隔
- **与规则1.8（运动员冲突）**：预赛和决赛都需要检查运动员时间冲突

#### 实际运行表现

**测试数据统计**（220条赛次）：
- 直接决赛：约60%（报名人数少或项目规模小）
- 预赛+决赛：约40%（主要是短跑、跳远等热门项目）

**用户反馈**：
- 自动判断减少手动配置，符合传统运动会规则
- 决赛队伍数可在项目定义中灵活配置
- AI优化阶段不会改变预决赛关系（不可变约束）

---

### 1.4 分组规则

**算法**（`split_into_groups(n: int, lanes: int)`）：

1. **基本规则**：按满跑道数（lanes）分组
2. **特殊处理**：若末组人数 < 跑道数的一半
   - 将末组与上一组合并
   - 合并后的人数均分为两组
   - 避免出现人数过少的尾组

**示例**：
```python
lanes = 6, 报名 20 人
常规分组：[6, 6, 6, 2]
实际分组：[6, 6, 4, 4]  # 末组2人<3人，与上组合并为8人，均分
```

**详细说明**：

#### 设计背景

**为什么要避免小尾组？**
1. **竞技公平性**：
   - 2人一组 vs 6人一组，竞争激烈程度差异大
   - 小组可能出现"保送"现象（如预赛取前8名，最后一组只有2人全部晋级）

2. **观赏性**：
   - 6道跑道只有2人比赛，场面冷清
   - 观众和运动员都感觉不完整

3. **计时公平**：
   - 人少的组心理压力小，可能跑出更好成绩
   - 人多的组竞争激烈，可能影响发挥

4. **裁判效率**：
   - 小组准备时间与满组相同，但产出比赛数据少
   - 分组更均衡可以优化整体时间利用

#### 实现细节

```python
def split_into_groups(n: int, lanes: int) -> list[int]:
    """返回每组人数列表，如 [6, 6, 4, 4] 表示4组"""
    if n <= 0:
        return []
    if n <= lanes:
        return [n]  # 只需1组
    
    full = n // lanes        # 满组数量
    rem = n % lanes          # 剩余人数
    sizes = [lanes] * full   # 先生成满组
    
    if rem == 0:
        return sizes  # 刚好整除，无剩余
    
    # 关键逻辑：判断剩余人数是否过少
    if rem < math.ceil(lanes / 2):
        # 剩余人数 < 半条跑道（如6道跑道，rem<3）
        # 策略：与上一个满组合并后均分为两组
        merged = sizes.pop() + rem  # 取出最后一个满组 + 剩余人数
        first = math.ceil(merged / 2)  # 上取整，确保不遗漏
        second = merged - first
        sizes.extend([first, second])
    else:
        # 剩余人数 >= 半条跑道，可以独立成组
        sizes.append(rem)
    
    return sizes
```

#### 算法逻辑详解

**步骤1：判断简单情况**
```python
if n <= lanes:
    return [n]  # 报名10人，跑道6道 → [10]，虽然超道但单组
```
- 实际上这种情况下，10人会分配到6道 + 4道备用位置
- 或者部分运动员站在跑道外侧（田赛项目）

**步骤2：计算满组和剩余**
```python
full = n // lanes  # 整除得到满组数
rem = n % lanes    # 取余得到剩余人数
```

**示例**：
- 20人，6道 → full=3, rem=2
- 25人，6道 → full=4, rem=1
- 18人，6道 → full=3, rem=0

**步骤3：处理剩余人数**

**情况A：剩余 = 0**
```python
if rem == 0:
    return sizes  # [6, 6, 6] 刚好整除
```

**情况B：剩余 < 半条跑道**
```python
if rem < math.ceil(lanes / 2):  # 如 6道，rem<3
    # rem=1或2时触发
    merged = sizes.pop() + rem  # 6 + 2 = 8
    first = math.ceil(merged / 2)  # ceil(8/2) = 4
    second = merged - first        # 8 - 4 = 4
    sizes.extend([4, 4])
    # 最终：[6, 6, 4, 4]
```

**为什么用 `math.ceil(lanes / 2)` 作为阈值？**
- 6道跑道：阈值 = ceil(6/2) = 3
  - rem=1或2 → 触发合并（过少）
  - rem=3,4,5 → 独立成组（足够）
  
- 8道跑道：阈值 = ceil(8/2) = 4
  - rem=1,2,3 → 触发合并
  - rem=4,5,6,7 → 独立成组

**情况C：剩余 >= 半条跑道**
```python
else:
    sizes.append(rem)  # 直接作为最后一组
    # 示例：[6, 6, 6, 4]
```

#### 各种情况举例

| 报名人数 | 跑道数 | 满组 | 剩余 | 阈值 | 分组结果 | 说明 |
|---------|--------|------|------|------|---------|------|
| 20 | 6 | 3 | 2 | 3 | [6,6,4,4] | 2<3，合并 |
| 25 | 6 | 4 | 1 | 3 | [6,6,6,4,4] | 1<3，合并 |
| 22 | 6 | 3 | 4 | 3 | [6,6,6,4] | 4≥3，独立 |
| 18 | 6 | 3 | 0 | 3 | [6,6,6] | 整除 |
| 30 | 8 | 3 | 6 | 4 | [8,8,8,6] | 6≥4，独立 |
| 27 | 8 | 3 | 3 | 4 | [8,8,6,6] | 3<4，合并 |
| 10 | 6 | 1 | 4 | 3 | [6,4] | 4≥3，独立 |
| 8 | 6 | 1 | 2 | 3 | [5,3] | 2<3，合并 |

#### 边界情况

**1. 极小报名（n ≤ lanes）**
```python
# 3人报名，6道跑道
split_into_groups(3, 6)  # → [3]
```
- 返回单组，人数即报名数
- 实际比赛时3人占用3道

**2. 极大报名（如100人）**
```python
split_into_groups(100, 6)
# 100 ÷ 6 = 16 满组 + 4 剩余
# 4 ≥ 3 → 独立成组
# → [6,6,6,6,6,6,6,6,6,6,6,6,6,6,6,6,4]
```

**3. 剩余 = 1（最极端）**
```python
split_into_groups(13, 6)
# 13 ÷ 6 = 2 满组 + 1 剩余
# 1 < 3 → 合并
# merged = 6 + 1 = 7
# first = ceil(7/2) = 4
# second = 7 - 4 = 3
# → [6, 4, 3]
```

**4. 刚好半跑道（边界值）**
```python
split_into_groups(15, 6)
# 15 ÷ 6 = 2 满组 + 3 剩余
# 3 = 3 → 不触发合并（≥阈值）
# → [6, 6, 3]
```

#### 特殊项目的适配

**田赛项目（跳远、跳高）**
- 跑道数概念变为"比赛席位数"或"同时比赛人数"
- 跳远：通常6-8人一组轮流跳
- 分组逻辑相同，确保每组人数均衡

**团队项目（接力）**
- `n` 是班级数而非人数
- `lanes` 是跑道数（班级队伍数）
- 算法完全适用

#### 分组后的填充

```python
def _fill_groups(entry: ScheduleEntry, participants: list, 
                 is_team: bool, lanes: int, db: Session):
    sizes = split_into_groups(len(participants), lanes)
    entry.group_count = len(sizes)  # 记录组数
    
    cursor = 0
    for g, size in enumerate(sizes):
        grp = ScheduleGroup(group_no=g + 1)  # 组号从1开始
        chunk = participants[cursor : cursor + size]
        cursor += size
        
        for i, p in enumerate(chunk):
            # 创建 ScheduleLane 记录（道次分配）
            grp.lanes.append(
                ScheduleLane(lane_no=i + 1, athlete_id=p.id)
            )
```

**关键点**：
- `participants` 已按号码或年级班级排序（稳定顺序）
- 依次切片填充到各组
- 每组内的 `lane_no` 从1开始编号

#### 与其他规则的交互

- **与规则1.3（预决赛判断）**：预赛和决赛都用此算法分组
- **与规则1.5（项目组间隔）**：组数直接影响项目总耗时（组数×组间隔）
- **与规则1.8（运动员冲突）**：分组后的人员分布影响冲突检测范围

#### 实际运行表现

**常见场景**：
- 30人项目（如三年级男100米）：6道 → [6,6,6,6,6] 5组
- 22人项目（如四年级女跳远）：6道 → [6,6,6,4] 4组（不触发合并）
- 20人项目（如五年级男60米）：6道 → [6,6,4,4] 4组（触发合并）

**优化效果**：
- 避免了"6,6,6,6,2"这样的极不均衡分组
- 每组人数差距最多2人（如4人组vs6人组）
- 减少了"保送"现象，提升竞技公平性

**用户反馈**：
- 教练认可均衡分组，减少争议
- 裁判认为合并后时间利用更高效
- 观众感受到比赛更完整连贯

---

### 1.5 项目组间隔时间

**配置**（`EVENT_INTERVAL_MINUTES`）：

| 项目名称 | 间隔时间（分钟） | 项目名称 | 间隔时间（分钟） |
|---------|----------------|---------|----------------|
| 60米 | 2 | 100米 | 3 |
| 800米 | 5 | 1000米 | 5 |
| 100*2米 | 6 | 100*4米 | 4 |
| 播种与收割 | 15 | 十人抓杆 | 15 |
| 垒球 | 30 | 跳高 | 30 |
| 跳远 | 30 | 袋鼠跳 | 15 |
| 师生同乐 | 10 | 侧向推实心球 | 30 |

**应用逻辑**（`_estimate_times_with_intervals()`）：
```python
duration = max(5, group_count * interval)  # 组数 × 间隔，最少5分钟
```

**注意**：
- 尽可能满足间隔要求
- 时间不够时自动压缩，均匀分配剩余时间

**详细说明**：

#### 设计背景

**为什么需要组间隔时间？**

1. **裁判记录与统计**：
   - 每组结束后需要记录成绩、核对数据
   - 短跑项目：计时员核对手表、电子计时设备
   - 田赛项目：测量员记录距离、高度

2. **运动员准备与恢复**：
   - 检录组织下一组运动员入场
   - 运动员热身、调整起跑器
   - 田赛项目试跳、试投

3. **场地器材调整**：
   - 跳高：调整横杆高度（每次3-5厘米）
   - 跳远：平整沙坑、擦拭踏板
   - 短跑：清理跑道、重置起跑器

4. **广播与仪式**：
   - 报幕下一组运动员名单
   - 宣布上一组成绩
   - 颁奖仪式（决赛）

#### 间隔时间配置原则

**短跑类（2-6分钟）**：
```python
"60米": 2,    # 最短距离，准备快
"100米": 3,   # 经典短跑，需要更仔细的起跑准备
"100*2米": 6, # 接力需交接棒、确认区域
"100*4米": 4, # 接力但队伍少
```

**设计考量**：
- 60米：小学低年级项目，距离短，成绩记录快
- 100米：标准短跑，起跑器调整、反应时检测
- 接力：需要交接棒练习、确认接力区、团队协调

**中长跑类（5分钟）**：
```python
"800米": 5,
"1000米": 5,
```

**设计考量**：
- 比赛时间长（2-4分钟/组），运动员恢复需要时间
- 终点记录员需要更多时间记录多人成绩
- 下一组需要充分热身（避免抽筋）

**跳跃与投掷类（30分钟）**：
```python
"跳高": 30,   # 逐渐升高，每组可能多轮
"跳远": 30,   # 每人多次试跳
"垒球": 30,
"侧向推实心球": 30,
```

**设计考量**：
- **跳高特殊性**：
  - 不是简单的"一组跳完换下一组"
  - 同一高度所有人轮流跳，失败者淘汰
  - 横杆逐渐升高，可能持续1-2小时
  - 30分钟是单个高度的平均时间
  
- **跳远**：
  - 每人通常3次试跳（预赛）或6次（决赛）
  - 需要测量、记录每次成绩
  - 沙坑平整耗时
  
- **投掷**：
  - 安全考量：确保区域无人才能投掷
  - 捡球、测量、记录
  - 每人多次试投

**趣味与团队项目（10-15分钟）**：
```python
"袋鼠跳": 15,
"师生同乐": 10,
"播种与收割": 15,
"十人抓杆": 15,
```

**设计考量**：
- 规则解释与示范
- 器材准备（袋子、杆子、道具）
- 参与人数多，组织复杂
- 娱乐性项目，允许更宽松的时间

#### 实现细节

**时间估算函数**：
```python
def _estimate_times_with_intervals(db: Session, year_id: int):
    # 按半天窗口分组
    for (day, period), items in buckets.items():
        win_start = 8*60 if period == "上午" else 14*60  # 分钟数
        win_end = 12*60 if period == "上午" else 18*60
        
        current_time = win_start
        for i, entry in enumerate(items):
            ev = db.get(Event, entry.event_id)
            
            # 获取该项目的间隔时间
            interval = EVENT_INTERVAL_MINUTES.get(ev.name, 0)
            
            # 估算持续时间：组数 × 间隔
            duration = max(5, entry.group_count * interval) if interval > 0 else 15
            
            # 计算开始和结束时间
            start_time = current_time
            end_time = min(current_time + duration, win_end)
            
            # 超出窗口则压缩
            if end_time > win_end:
                end_time = win_end
                if i == len(items) - 1:  # 最后一个赛次
                    start_time = max(win_start, win_end - duration)
            
            entry.start_time = f"{start_time//60:02d}:{start_time%60:02d}"
            entry.end_time = f"{end_time//60:02d}:{end_time%60:02d}"
            
            current_time = end_time  # 移动游标
```

**关键点**：

1. **基础计算**：
```python
duration = max(5, group_count * interval)
```
- 组数 = 5，间隔 = 3分钟 → 持续 15分钟
- 组数 = 1，间隔 = 3分钟 → 持续 5分钟（最小保证）
- 间隔 = 0（未配置项目）→ 默认 15分钟

2. **时间窗口约束**：
```python
end_time = min(current_time + duration, win_end)
```
- 不超过半天窗口结束时间
- 上午：08:00-12:00 (240分钟)
- 下午：14:00-18:00 (240分钟)

3. **窗口溢出处理**：
```python
if current_time >= win_end and i < len(items) - 1:
    remaining = len(items) - i - 1
    slot_size = 10  # 最小10分钟
    for j, remaining_entry in enumerate(items[i+1:], 1):
        r_start = current_time + (j-1) * slot_size
        r_end = r_start + slot_size
        if r_end > win_end:
            r_end = win_end
        remaining_entry.start_time = f"{r_start//60:02d}:{r_start%60:02d}"
        remaining_entry.end_time = f"{r_end//60:02d}:{r_end%60:02d}"
```
- 时间不够时，剩余赛次均匀压缩
- 每个赛次最少 10分钟

#### 各种情况举例

**情况1：时间充足**
```
半天窗口：08:00-12:00 (240分钟)
赛次安排：
  1. 三年级男100米 (5组 × 3分钟 = 15分钟) → 08:00-08:15
  2. 三年级女100米 (4组 × 3分钟 = 12分钟) → 08:15-08:27
  3. 四年级男跳远 (3组 × 30分钟 = 90分钟) → 08:27-09:57
  4. 四年级女跳远 (3组 × 30分钟 = 90分钟) → 09:57-11:27
  
剩余时间：11:27-12:00 (33分钟) 可安排其他项目
```

**情况2：时间紧张**
```
半天窗口：14:00-18:00 (240分钟)
赛次总需求：
  - 跳高 (5组 × 30 = 150分钟)
  - 100米预赛 (8组 × 3 = 24分钟)
  - 800米 (6组 × 5 = 30分钟)
  - 接力 (4组 × 6 = 24分钟)
  总计：228分钟（接近上限）

实际执行：
  1. 跳高 14:00-16:30 (150分钟，不同场地可并行)
  2. 100米 14:00-14:24 (24分钟，主跑道)
  3. 800米 14:24-14:54 (30分钟)
  4. 接力 14:54-15:18 (24分钟)
  
若跳高与短跑同场地，则需串行，可能超时
```

**情况3：严重超时（触发压缩）**
```
半天窗口：08:00-12:00 (240分钟)
10个赛次，每个理想30分钟 = 300分钟（超60分钟）

系统处理：
  - 前7个赛次正常分配（0-210分钟）
  - 剩余3个赛次压缩到 210-240分钟
  - 每个赛次 10分钟（最小单位）
  
实际时间：
  08:00-09:30  (前3个)
  09:30-10:30  (中间2个)
  10:30-11:00  (第6个)
  11:00-11:30  (第7个)
  11:30-11:40  (第8个，压缩)
  11:40-11:50  (第9个，压缩)
  11:50-12:00  (第10个，压缩)
```

#### 特殊项目处理

**跳高的特殊性**：
```python
"跳高": 30  # 实际可能持续1-2小时
```

**实际情况**：
- 跳高不是"5组×30分钟=150分钟"的简单模型
- 而是"起始高度 → 逐渐升高 → 最后决出冠军"
- 可能所有运动员在同一"虚拟组"中竞争
- 30分钟是平均估算，实际由现场裁判控制

**系统建议**：
- 将跳高安排在独立时段
- 与其他项目并行（不同场地）
- 或安排在下午较晚时段，允许延时

**接力的特殊性**：
```python
"100*4米": 4  # 每组4分钟
"100*2米": 6  # 每组6分钟（为什么更长？）
```

**原因分析**：
- 100×2米：小学低年级，交接棒不熟练，需要更多时间
- 100×4米：初高中，交接棒熟练，但距离更长

#### 未配置项目的默认值

```python
interval = EVENT_INTERVAL_MINUTES.get(ev.name, 0)
duration = max(5, group_count * interval) if interval > 0 else 15
```

**逻辑**：
- 若 `ev.name` 不在字典中，`interval = 0`
- `interval > 0` 为 False，使用默认 15分钟
- 这是一个保守估计，适用于大多数未知项目

#### 与其他规则的交互

- **与规则1.2（项目排序）**：高优先级项目间隔通常较短，可快速完成
- **与规则1.4（分组）**：组数直接影响总时间（组数越多越久）
- **与规则1.7（场地并行）**：不同场地可同时进行，时间窗口压力减小
- **与规则1.8（运动员冲突）**：间隔时间影响运动员休息时长

#### 配置扩展指南

**添加新项目间隔**：
```python
# 在 schedule_gen.py 第48-63行
EVENT_INTERVAL_MINUTES = {
    "60米": 2,
    "新项目名称": 间隔分钟数,  # ← 添加这里
    # ...
}
```

**同步更新位置**：
1. `schedule_rules.py` 的 `DEFAULT_HARD_RULES` 第5条
2. `schedule_ai.py` 的 `ARRANGE_SYSTEM` 规则7
3. `schedule_ai.py` 的 `CHECK_SYSTEM` 维度3

**配置建议**：
- 短距离跑：2-3分钟
- 中长距离跑：5分钟
- 跳跃、投掷：20-30分钟
- 趣味项目：10-15分钟
- 团队接力：4-6分钟

#### 实际运行表现

**测试数据反馈**：
- 上午4小时窗口通常可安排 8-12个赛次
- 跳跃、投掷类项目占用时间最长
- 短跑类项目可以快速连续完成

**用户反馈**：
- 裁判认为间隔时间基本合理，偶尔需要微调
- AI 优化可以根据"时间太紧"等自然语言调整
- 手动编辑功能可以精确修改每个赛次时间

**优化建议**：
- 将耗时长的项目（跳高）安排在独立场地
- 短跑类项目集中安排，提高效率
- 预留10-15%时间buffer应对突发情况

---

### 1.6 预赛决赛间隔

**规则**：同一项目的预赛与决赛至少相隔半天（一个时段）

**实现**：`_ensure_prelim_final_gap()`
- 如果决赛在预赛之前或同一半天
- 自动将决赛移到预赛后至少一个时段

**详细说明**：

#### 设计背景

**为什么需要预决赛间隔？**

1. **运动员恢复时间**：
   - 短跑项目：虽然比赛时间短（10-30秒），但对肌肉爆发力消耗极大
   - 中长跑：需要更长恢复时间，乳酸清除需要数小时
   - 跳跃项目：关节和肌腱需要休息，避免受伤
   - **最少半天**（3-4小时）是运动医学的基本要求

2. **心理准备与战术调整**：
   - 运动员需要时间分析预赛成绩
   - 教练根据预赛表现调整战术
   - 准备决赛的心理状态（预赛求稳，决赛求胜）

3. **成绩统计与名单确认**：
   - 预赛结束后需要统计所有组成绩
   - 按成绩排序取前N名进决赛
   - 核对名单、打印决赛道次表
   - 通知运动员决赛时间和道次
   - 这个流程需要至少1-2小时

4. **比赛公平性**：
   - 所有晋级运动员都应有相同的恢复时间
   - 避免"上午预赛的选手比下午预赛的选手休息更久"

#### 实现细节

```python
def _ensure_prelim_final_gap(db, year_id, seg_entries, slots_map):
    """确保同一项目的预赛与决赛至少相隔半天"""
    for seg, entries in seg_entries.items():
        slots = slots_map[seg]  # 该赛段的半天槽位列表
        event_rounds = defaultdict(list)  # {event_id: [(entry, ev), ...]}
        
        # 按项目ID分组
        for ev, entry in entries:
            event_rounds[ev.id].append((entry, ev))
        
        # 检查每个项目的预决赛关系
        for event_id, rounds in event_rounds.items():
            if len(rounds) < 2:
                continue  # 只有1个赛次（直接决赛），无需检查
            
            # 找出预赛和决赛
            prelim = next((e for e, _ in rounds if e.round_type == "预赛"), None)
            final = next((e for e, _ in rounds if e.round_type == "决赛"), None)
            
            if not prelim or not final:
                continue  # 不是预决赛结构，跳过
            
            # 获取当前槽位
            prelim_slot = (prelim.day_index, prelim.period)
            final_slot = (final.day_index, final.period)
            
            # 计算槽位索引
            prelim_idx = slots.index(prelim_slot) if prelim_slot in slots else -1
            final_idx = slots.index(final_slot) if final_slot in slots else -1
            
            # 关键判断：决赛槽位 <= 预赛槽位？
            if final_idx <= prelim_idx:
                # 违反规则！决赛在预赛之前或同一时段
                # 调整：将决赛移到预赛后至少一个槽位
                target_idx = min(prelim_idx + 1, len(slots) - 1)
                if target_idx > prelim_idx:
                    final.day_index, final.period = slots[target_idx]
                    # 如果没有更后的槽位（已是最后一个），保持原位但会在校验时报警
```

**关键逻辑**：

1. **槽位索引化**：
```python
slots = [(1, "上午"), (1, "下午"), (2, "上午")]
# 索引:    0           1            2

prelim_slot = (1, "上午")  # 预赛在第1天上午
prelim_idx = 0

final_slot = (1, "下午")   # 决赛在第1天下午
final_idx = 1

# 判断: final_idx(1) > prelim_idx(0) ✓ 满足间隔要求
```

2. **违规判断**：
```python
if final_idx <= prelim_idx:  # 决赛索引 <= 预赛索引
    # 情况A: final_idx < prelim_idx → 决赛在预赛之前（时间倒置）
    # 情况B: final_idx == prelim_idx → 决赛和预赛同一半天（间隔不足）
```

3. **调整策略**：
```python
target_idx = min(prelim_idx + 1, len(slots) - 1)
# prelim_idx + 1: 预赛后一个槽位
# len(slots) - 1: 最后一个槽位（防止越界）
# min(): 取两者较小值

if target_idx > prelim_idx:  # 确实有更后的槽位
    final.day_index, final.period = slots[target_idx]
```

#### 各种情况举例

**情况1：正常间隔（无需调整）**
```
赛段：小学（2天赛程）
槽位：[(1, "上午"), (1, "下午")]
索引：    0             1

项目：三年级男100米
预赛：(1, "上午") → idx=0
决赛：(1, "下午") → idx=1

判断：1 > 0 ✓ 满足规则，无需调整
```

**情况2：同一半天（需要调整）**
```
赛段：初高中（3天赛程）
槽位：[(2, "下午"), (3, "上午"), (3, "下午")]
索引：    0             1            2

项目：初一男800米
预赛：(2, "下午") → idx=0
决赛：(2, "下午") → idx=0  ← 同一时段！

判断：0 <= 0 → 违规
调整：target_idx = min(0+1, 2) = 1
      决赛移到 (3, "上午")
```

**情况3：时间倒置（需要调整）**
```
赛段：小学（2天赛程）
槽位：[(1, "上午"), (1, "下午")]
索引：    0             1

项目：五年级女跳远
预赛：(1, "下午") → idx=1
决赛：(1, "上午") → idx=0  ← 决赛在预赛之前！

判断：0 < 1 → 违规（时间倒置）
调整：target_idx = min(1+1, 1) = 1
      决赛移到 (1, "下午")  ← 移到预赛同时段（仍违规）

问题：只有2个槽位，无法满足"至少间隔半天"
解决：系统会在后续校验中报警，建议用户手动调整或增加比赛天数
```

**情况4：预赛在最后一个槽位（无法调整）**
```
赛段：初高中（3天赛程）
槽位：[(2, "下午"), (3, "上午"), (3, "下午")]
索引：    0             1            2

项目：高二男1500米
预赛：(3, "下午") → idx=2  ← 最后一个槽位
决赛：(3, "上午") → idx=1  ← 在预赛之前

判断：1 < 2 → 违规
调整：target_idx = min(2+1, 2) = 2
      决赛移到 (3, "下午")  ← 与预赛同时段（仍违规）

问题：预赛已在最后槽位，无更后的槽位可用
解决：系统报警，建议：
      1. 将预赛提前到 (3, "上午")
      2. 或增加比赛天数
      3. 或取消预赛，直接决赛
```

#### 边界情况

**1. 直接决赛（无预赛）**
```python
if len(rounds) < 2:
    continue  # 只有1个赛次，无需检查
```
- 报名人数少，直接决赛
- 不存在预决赛间隔问题

**2. 特殊赛制（半决赛）**
- 当前系统只支持预赛+决赛两级
- 如果未来扩展支持半决赛，需要修改逻辑：
  - 预赛→半决赛：至少半天
  - 半决赛→决赛：至少半天

**3. 多赛段项目（跨年级）**
- 示例："小学vs初中接力"
- 当前系统不支持跨赛段项目
- 需要手动归类到某个赛段

**4. 槽位不足**
```python
if target_idx > prelim_idx:
    final.day_index, final.period = slots[target_idx]
# else: 无可用槽位，保持原位，后续校验会报警
```

#### 与其他规则的交互

**与规则1.1（赛段分配）**：
- 在赛段内检查预决赛间隔
- 小学预赛（第1天上午）→ 决赛（第1天下午）✓
- 不会出现小学预赛→初高中决赛的跨赛段情况

**与规则1.8（运动员冲突）**：
- 预决赛间隔为运动员提供恢复时间
- 减少了同一运动员在短时间内多次高强度比赛的可能
- 但仍需检查决赛是否与其他项目冲突

**与规则1.5（项目组间隔）**：
- 组间隔（分钟级）vs 预决赛间隔（半天级）
- 两者作用范围不同，互不冲突

#### 实际运行表现

**测试数据分析**（220条赛次）：
- 有预决赛结构的项目：约40%
- 自动调整的情况：约5-10%（初次生成时可能出现）
- 调整后仍违规：约1-2%（槽位不足）

**典型调整案例**：
```
项目：四年级男100米
初次生成：
  预赛：第1天上午
  决赛：第1天上午  ← 同时段

自动调整后：
  预赛：第1天上午
  决赛：第1天下午  ✓

用户满意度：高（符合传统运动会安排）
```

**用户反馈**：
- 教练认为半天间隔是合理的最小值
- 对于重要项目（如100米决赛），建议隔得更久（如隔一整天）
- AI 优化可以响应"把100米决赛移到最后一天"等需求

#### 扩展建议

**1. 配置化间隔要求**：
```python
PRELIM_FINAL_GAP = {
    "100米": 2,  # 至少隔2个半天（1整天）
    "800米": 3,  # 至少隔3个半天（1.5天）
    "default": 1,  # 默认至少隔1个半天
}
```

**2. 智能分配优先级**：
- 重要项目（100米、接力）的预赛尽量排在前面
- 为决赛预留后面的"黄金时段"

**3. 多级赛制支持**：
```
预赛 → 半决赛 → 决赛
  ↓       ↓       ↓
半天    半天    最后一天
```

**4. 用户自定义**：
- 在项目定义中增加 `min_gap_halfdays` 字段
- 灵活配置不同项目的间隔要求

---

### 1.7 场地与并行规则

**规则**：
- 同一场地同一时刻只能开展一项比赛
- **不同场地可以同时进行不同项目**（关键特性）

**场地来源**：项目定义中的 `venue` 字段

**详细说明**：

#### 设计背景

**为什么需要场地并行？**

1. **时间效率最大化**：
   - 传统运动会：所有项目串行，总时长 = ∑各项目时长
   - 场地并行：不同场地同时进行，总时长 ≈ max(各场地时长)
   - **时间节省可达 30-50%**

2. **场地资源充分利用**：
   - 学校通常有多个场地：主跑道、副跑道、跳远沙坑、跳高区、投掷区
   - 这些场地物理隔离，互不干扰
   - 不并行使用 = 资源浪费

3. **运动员体验优化**：
   - 不同场地的项目可同时进行，缩短整体赛程
   - 运动员等待时间减少
   - 避免"上午跳远，下午才轮到短跑"的长时间等待

4. **观众与组织便利**：
   - 观众可以选择观看不同场地的比赛
   - 裁判和志愿者分工明确，各管一块场地
   - 广播可以同时播报多个场地的进展

#### 场地定义

**项目创建时指定**：
```python
# Event 模型（backend/app/models/event.py）
class Event(Base):
    __tablename__ = "events"
    venue = Column(String(100), nullable=False, default="主跑道")
    # 场地名称：主跑道、副跑道、跳远沙坑、跳高区、投掷区等
```

**常见场地类型**：
| 场地名称 | 适用项目 | 特点 |
|---------|---------|------|
| 主跑道 | 60米、100米、200米、接力 | 标准8道，电子计时 |
| 副跑道 | 预赛、训练 | 6道，手动计时 |
| 跳远沙坑 | 跳远、三级跳 | 沙坑+助跑道 |
| 跳高区 | 跳高 | 跳高架+海绵垫 |
| 投掷区 | 垒球、实心球、铅球 | 隔离区域，确保安全 |
| 田径场中央 | 趣味项目、团体项目 | 草坪或平地 |
| 800米/1500米起点 | 中长跑 | 特殊起跑线 |

#### 并行规则实现

**1. 赛次生成时记录场地**：
```python
def build_skeleton(db: Session, year_id: int, config: ScheduleConfig):
    for ev in events:
        entry = ScheduleEntry(
            event_id=ev.id,
            venue=ev.venue,  # ← 从项目定义中继承场地
            # ...
        )
```

**2. 时间分配时允许并行**：
```python
def _assign_slots_and_times(db, year_id, config, seg_entries):
    # 按半天窗口分配时间
    # 不同场地的赛次可以有重叠的时间段
    # 例如：
    # 08:00-08:30  主跑道：100米预赛
    # 08:00-08:30  跳远沙坑：跳远预赛  ← 可以并行！
```

**3. 冲突检测时区分场地**：
```python
def _resolve_athlete_conflicts(db, year_id, seg_entries, slots_map):
    for aid, conflicted_entries in conflicts.items():
        slot_groups = defaultdict(list)
        for entry, slot in conflicted_entries:
            slot_groups[slot].append(entry)
        
        for slot, slot_entries in slot_groups.items():
            if len(slot_entries) <= 1:
                continue
            
            # 关键判断：检查场地
            venues = [db.get(Event, e.event_id).venue for e in slot_entries]
            if len(set(venues)) == len(venues):
                # 所有项目在不同场地，可以并行，不算冲突
                continue  # ← 跳过，不调整
            
            # 同一场地才需要调整
            # ...
```

**4. AI 优化时明确规则**：
```python
# schedule_ai.py 的 ARRANGE_SYSTEM 提示词
"3. 同场地同时刻不重叠，但不同场地可以同时进行不同项目"
```

**5. 后端校验时按场地分组**：
```python
def _validate(db: Session, year_id: int, events: list[Event], regs: dict):
    # 同场地同天同时段时间重叠检测
    venue_slots: dict[tuple, list[tuple]] = {}
    for e in entries:
        key = (e.day_index, e.period, e.venue)  # ← 场地作为分组键
        venue_slots[key].append((e.start_time, e.end_time, e.id))
    
    for key, slots in venue_slots.items():
        # 只检查同一场地内的时间重叠
        for i in range(len(slots)):
            for j in range(i + 1, len(slots)):
                if _overlap(slots[i][0], slots[i][1], slots[j][0], slots[j][1]):
                    issues.append(f"场地「{key[2]}」第{key[0]}天{key[1]}存在时间重叠")
```

#### 并行调度示例

**示例1：理想并行**
```
第1天上午 (08:00-12:00)

主跑道：
  08:00-08:15  三年级男100米预赛 (5组×3分钟)
  08:15-08:30  三年级女100米预赛 (4组×3分钟)
  08:30-08:50  四年级男100米预赛

跳远沙坑：
  08:00-09:30  三年级男跳远 (3组×30分钟)
  09:30-11:00  三年级女跳远 (3组×30分钟)

跳高区：
  08:00-10:30  四年级男跳高 (5组×30分钟)

总耗时：2.5小时
若串行：(15+12+20) + (90+90) + (150) = 377分钟 = 6.3小时
时间节省：(6.3 - 2.5) / 6.3 = 60%
```

**示例2：运动员冲突但场地不同**
```
运动员：张三（三年级）
报名项目：100米、跳远

时间安排：
  08:00-08:15  100米预赛（主跑道）
  08:00-08:30  跳远预赛第1组（跳远沙坑）

判断：
  - 时间重叠 ✓
  - 同一运动员 ✓
  - 但在不同场地 ✓

结果：
  - 旧逻辑：视为冲突，需要调整时间
  - 新逻辑（规则7优化）：不同场地，允许并行，不算冲突
  
实际处理：
  - 如果张三100米在第1组（08:00开始），且跳远在第1组（08:00开始）
  - 确实存在物理冲突（无法分身）
  - 系统会在更细粒度的检查中发现：同一运动员在同一时间段有比赛
  - 调整策略：将跳远移到08:15之后，或将张三调到100米第2组
```

**示例3：同场地冲突**
```
场地：主跑道

时间安排：
  08:00-08:15  三年级男100米预赛
  08:10-08:25  四年级男60米预赛  ← 时间重叠！

判断：
  - 同一场地（主跑道）
  - 时间重叠（08:10-08:15）

结果：违规，需要调整
调整方案：
  - 方案1：60米移到08:15之后
  - 方案2：60米移到副跑道（如果60米项目可以在副跑道进行）
```

#### 场地并行的边界情况

**1. 场地未设置或为空**
```python
venue = ev.venue or "未指定场地"
```
- 未指定场地视为独立场地
- 不与其他项目并行（保守策略）

**2. 同一项目不同赛次**
```
项目：三年级男100米
场地：主跑道

预赛：08:00-08:15（主跑道）
决赛：08:10-08:25（主跑道）← 理论上可以同场地，但时间重叠违规

判断：同项目同场地，时间重叠 → 违规
```

**3. 场地容量限制**
- 当前系统不考虑场地容量（如主跑道是否同时容纳2个项目）
- 假设每个场地同一时刻只能进行1项比赛
- 扩展：可以引入"场地道次"概念，如主跑道1-4道、5-8道分别进行不同项目

**4. 特殊场地（如中长跑）**
```
项目：800米
场地：800米起点

特点：
  - 起跑线特殊（错位起跑）
  - 使用整个跑道（包括主跑道）
  - 与主跑道的短跑项目不能并行

当前处理：
  - 800米场地标记为"主跑道"或"800米起点"
  - 与主跑道项目视为同场地（不能并行）
  - 或标记为独立场地（允许与副跑道并行）
```

#### 与其他规则的交互

**与规则1.5（项目组间隔）**：
- 场地并行不影响单个场地内的组间隔要求
- 主跑道100米各组仍需间隔3分钟
- 跳远沙坑各组仍需间隔30分钟

**与规则1.8（运动员冲突）**：
- 不同场地并行时仍需检查运动员是否兼项
- 场地不同 ≠ 一定无冲突
- 需要检查具体的分组分道，确认运动员是否在同一时间比赛

**与规则2.1（赛段分配）**：
- 各赛段内部独立使用场地并行
- 小学赛段和初高中赛段在不同天，场地不冲突

#### 实际运行表现

**测试数据统计**（220条赛次）：
- 主跑道项目：约80条（短跑、接力）
- 跳远沙坑：约40条
- 跳高区：约30条
- 投掷区：约20条
- 其他：约50条

**并行效果分析**：
```
第1天上午（小学）：
  - 串行总时长：约6小时（超出4小时窗口）
  - 并行总时长：约3.5小时（利用3个主要场地）
  - 时间利用率：约88%（3.5/4）
```

**用户反馈**：
- 裁判长高度认可，大幅缩短赛程
- 部分教练反馈：需要注意运动员兼项安排
- 广播员建议：增加场地标识，方便播报

#### 优化建议

**1. 场地优先级配置**：
```python
VENUE_PRIORITY = {
    "主跑道": 1,     # 最重要，优先分配
    "副跑道": 2,
    "跳远沙坑": 1,
    "跳高区": 2,
    "投掷区": 3,     # 安全要求高，独立时段
}
```

**2. 智能场地分配**：
- 高优先级项目（100米决赛）分配到主跑道
- 预赛可以分配到副跑道
- 系统根据场地优先级和项目重要性自动匹配

**3. 场地切换成本**：
```python
VENUE_SWITCH_COST = {
    ("主跑道", "副跑道"): 5,   # 5分钟步行时间
    ("主跑道", "跳远沙坑"): 10,
    # ...
}
```
- 考虑运动员在不同场地间移动的时间
- 优化连续项目的场地安排

**4. 可视化展示**：
```
前端时间轴视图：
  主跑道    |====100米====|====60米====|
  跳远沙坑  |==========跳远==========|
  跳高区    |=====跳高=====|
           08:00      08:30      09:00
```
- 直观展示各场地的时间占用
- 方便用户发现并行优化空间

---

### 1.8 运动员冲突解决策略

**优先级**（`_resolve_athlete_conflicts()`）：

1. **策略1**：先尝试调整分组避免冲突（标记需要重新分组）
2. **策略2**：检查是否为不同场地
   - 如果冲突项目在不同场地 → 可以并行，不算冲突
   - 如果在同一场地 → 需要调整
3. **策略3**：调整项目顺序（修改 `order_no`）
4. **策略4**：移动到下一个时段（最后手段）

**代码片段**：
```python
# 检查场地并行
venues = [db.get(Event, e.event_id).venue for e in slot_entries]
if len(set(venues)) == len(venues):
    # 所有项目在不同场地，可以并行，不算冲突
    continue
```

**详细说明**：

#### 设计背景

**什么是运动员冲突？**
- 同一运动员在同一时间段被分配到多个项目
- 物理上无法分身，必须调整

**为什么会产生冲突？**
1. **学生兼项常见**：
   - 短跑好的学生通常报名 60米、100米、4×100米接力
   - 体能好的学生报名 800米、1500米
   - 全能型学生报名跳远+短跑

2. **初次生成按规则排序**：
   - 按项目优先级、年级、项目名排序
   - 不考虑个人兼项情况
   - 可能将同一学生的多个项目排在同一时段

3. **时间窗口有限**：
   - 上午/下午各4小时
   - 项目多、时间紧，难以完全避免冲突

**为什么需要分级策略？**
- **策略1（分组调整）**最温和，不打乱整体日程
- **策略4（移动时段）**影响大，可能引发连锁调整
- 逐级尝试，找到最小影响的解决方案

#### 实现细节

```python
def _resolve_athlete_conflicts(db, year_id, seg_entries, slots_map):
    """检查并解决运动员在同一时间段参加多个项目的冲突"""
    
    entries = db.query(ScheduleEntry).filter(
        ScheduleEntry.academic_year_id == year_id
    ).all()
    
    # 第一步：构建运动员时间槽位映射
    athlete_slots = defaultdict(list)  # {athlete_id: [(entry, slot), ...]}
    
    for entry in entries:
        slot = (entry.day_index, entry.period)  # 时间槽位
        for grp in entry.groups:
            for ln in grp.lanes:
                if ln.athlete_id:
                    athlete_slots[ln.athlete_id].append((entry, slot))
    
    # 第二步：找出冲突的运动员
    conflicts = {
        aid: slots 
        for aid, slots in athlete_slots.items() 
        if len(set(s for _, s in slots)) < len(slots)
    }
    # 逻辑：如果 unique(slots) < total(slots)，说明有重复槽位（冲突）
    
    if not conflicts:
        return  # 无冲突，结束
    
    # 第三步：逐个解决冲突
    for aid, conflicted_entries in conflicts.items():
        # 按槽位分组
        slot_groups = defaultdict(list)
        for entry, slot in conflicted_entries:
            slot_groups[slot].append(entry)
        
        # 找到有多个赛次的槽位
        for slot, slot_entries in slot_groups.items():
            if len(slot_entries) <= 1:
                continue  # 该槽位只有1个赛次，无冲突
            
            # 确定赛段和可用槽位
            entry = slot_entries[0]
            ev = db.get(Event, entry.event_id)
            seg = grade_segment(ev.group_name)
            slots = slots_map[seg]
            
            current_idx = slots.index(slot) if slot in slots else -1
            if current_idx < 0:
                continue
            
            # 【策略2】：检查场地
            venues = [db.get(Event, e.event_id).venue for e in slot_entries]
            if len(set(venues)) == len(venues):
                # 所有项目在不同场地，可以并行，不算冲突
                continue  # ← 跳过调整
            
            # 【策略3 & 4】：调整顺序或移动时段
            # 保留第一个赛次不动，调整后续赛次
            for i, conflicted_entry in enumerate(slot_entries[1:], 1):
                target_idx = min(current_idx + i, len(slots) - 1)
                if target_idx != current_idx:
                    # 移到下一个时段
                    conflicted_entry.day_index, conflicted_entry.period = slots[target_idx]
                else:
                    # 无法移动（已是最后槽位），记录为未解决冲突
                    unresolved_conflicts.append((aid, conflicted_entry))
```

#### 策略详解

**策略1：调整分组（当前版本未完全实现）**

**理想逻辑**：
```python
# 运动员张三同时在两个项目的同一时段：
#   - 100米预赛第1组（08:00-08:03）
#   - 跳远预赛第1组（08:00-08:30）

# 调整方案：将张三调到100米第2组（08:03-08:06）
# 这样张三可以：
#   - 08:03-08:06 参加100米第2组
#   - 08:00-08:30 参加跳远（100米结束后立即赶往跳远）
```

**当前实现**：
- 标记需要重新分组的情况（`unresolved_conflicts`）
- 由于重新分组涉及复杂逻辑（打乱已有分道、影响其他运动员），暂时记录但不自动执行
- 建议用户手动调整或使用AI优化

**未来优化方向**：
```python
def _try_regroup(athlete_id, entry1, entry2):
    """尝试将运动员从一个组调到另一个组"""
    # 找到该运动员在entry1中的位置
    for grp in entry1.groups:
        for ln in grp.lanes:
            if ln.athlete_id == athlete_id:
                # 找到了，尝试移到entry1的其他组
                for other_grp in entry1.groups:
                    if other_grp != grp:
                        # 检查other_grp的时间是否与entry2冲突
                        # 如果不冲突，移动该运动员
                        pass
```

**策略2：场地并行检测**

**关键代码**：
```python
venues = [db.get(Event, e.event_id).venue for e in slot_entries]
if len(set(venues)) == len(venues):
    continue  # 所有项目在不同场地，可以并行
```

**逻辑解释**：
```python
# 示例1：两个项目，不同场地
venues = ["主跑道", "跳远沙坑"]
set(venues) = {"主跑道", "跳远沙坑"}  # 2个唯一值
len(set(venues)) = 2
len(venues) = 2
2 == 2 → True → 可以并行

# 示例2：两个项目，同一场地
venues = ["主跑道", "主跑道"]
set(venues) = {"主跑道"}  # 1个唯一值
len(set(venues)) = 1
len(venues) = 2
1 != 2 → False → 不能并行，需要调整

# 示例3：三个项目，部分同场地
venues = ["主跑道", "主跑道", "跳远沙坑"]
set(venues) = {"主跑道", "跳远沙坑"}  # 2个唯一值
len(set(venues)) = 2
len(venues) = 3
2 != 3 → False → 存在同场地冲突，需要调整
```

**细节考量**：
- 判断 `len(set(venues)) == len(venues)` 意味着每个场地只出现一次
- 如果有任何场地出现2次以上，说明有同场地冲突
- 即使只有2个项目同场地，也需要调整

**策略3：调整顺序（修改 order_no）**

**当前实现**：
```python
# 通过修改 order_no 改变赛次在时间轴上的位置
# 但在当前代码中，这个策略与策略4合并了
# 主要通过移动到下一个时段实现
```

**理想实现**：
```python
def _adjust_order_within_slot(entries, conflicted_entry):
    """在同一时段内调整顺序，使时间错开"""
    # 假设时段内有足够时间
    # 将conflicted_entry的开始时间延后
    # 例如：08:00-08:15 改为 08:15-08:30
    pass
```

**局限性**：
- 当前时间估算是批量完成的（`_estimate_times_with_intervals`）
- 单独调整一个赛次的时间可能与其他赛次重叠
- 需要更精细的时间调度算法

**策略4：移动到下一个时段**

**实现**：
```python
for i, conflicted_entry in enumerate(slot_entries[1:], 1):
    target_idx = min(current_idx + i, len(slots) - 1)
    if target_idx != current_idx:
        conflicted_entry.day_index, conflicted_entry.period = slots[target_idx]
    else:
        unresolved_conflicts.append((aid, conflicted_entry))
```

**逻辑**：
```python
# 假设 current_idx = 0（第1天上午）
# slot_entries = [entry_A, entry_B, entry_C]  # 3个冲突赛次

# entry_A: 保持不动（索引0，第1天上午）
# entry_B: i=1, target_idx = min(0+1, 3-1) = 1 → 第1天下午
# entry_C: i=2, target_idx = min(0+2, 3-1) = 2 → 第2天上午

# 结果：3个赛次分散到3个不同时段
```

**边界情况**：
```python
# 假设 current_idx = 2（已是最后槽位，第2天下午）
# slot_entries = [entry_A, entry_B]

# entry_A: 保持不动（索引2）
# entry_B: i=1, target_idx = min(2+1, 2) = 2 → 仍在第2天下午
# target_idx == current_idx → 无法移动 → 记录为未解决冲突
```

#### 各种情况举例

**情况1：不同场地，不调整**
```
运动员：张三
冲突项目：
  - 100米预赛（主跑道，08:00-08:15）
  - 跳远预赛（跳远沙坑，08:00-09:30）

判断：
  venues = ["主跑道", "跳远沙坑"]
  len(set(venues)) = 2
  len(venues) = 2
  2 == 2 → True

结果：场地不同，允许并行，不调整

实际问题：
  张三在08:00需要同时在两个场地，仍然冲突！
  
进一步检查：
  系统会在更细粒度检查中发现张三在100米第1组（08:00开始）
  如果跳远第1组也是08:00，需要将张三调到100米第2组或跳远第2组
```

**情况2：同一场地，移动时段**
```
运动员：李四
冲突项目：
  - 60米预赛（主跑道，08:00-08:10，第1天上午）
  - 100米预赛（主跑道，08:10-08:25，第1天上午）

判断：
  venues = ["主跑道", "主跑道"]
  len(set(venues)) = 1
  len(venues) = 2
  1 != 2 → False → 需要调整

调整：
  current_idx = 0（第1天上午）
  保留60米预赛在第1天上午
  将100米预赛移到 target_idx = 0+1 = 1（第1天下午）

结果：
  - 60米预赛：第1天上午
  - 100米预赛：第1天下午 ✓
```

**情况3：三个项目冲突**
```
运动员：王五
冲突项目（都在第1天上午）：
  - A项目（主跑道）
  - B项目（主跑道）
  - C项目（跳远沙坑）

判断：
  venues = ["主跑道", "主跑道", "跳远沙坑"]
  len(set(venues)) = 2
  len(venues) = 3
  2 != 3 → 有同场地冲突

调整：
  A项目：保持不动（第1天上午）
  B项目：移到第1天下午
  C项目：移到第2天上午

结果：三个项目分散到三个时段 ✓
```

**情况4：无可用槽位**
```
运动员：赵六
冲突项目（都在第2天下午，最后一个槽位）：
  - D项目
  - E项目

判断：同场地冲突，需要调整

调整：
  current_idx = 最后槽位索引
  D项目：保持不动
  E项目：target_idx = min(current_idx+1, len(slots)-1) = current_idx
  target_idx == current_idx → 无法移动

结果：记录为未解决冲突，建议：
  1. 增加比赛天数
  2. 将D或E项目提前到更早的时段
  3. 手动调整分组，避免赵六在同一组
```

#### 冲突检测的多层次

**层次1：槽位级别（半天）**
- 检查同一运动员是否在同一半天参加多个项目
- 粗粒度，可能产生误判（同半天但时间不重叠）

**层次2：时间级别（分钟）**
```python
def _validate(db: Session, year_id: int, events, regs):
    for aid, slots in athlete_slots.items():
        for i in range(len(slots)):
            for j in range(i + 1, len(slots)):
                a, b = slots[i], slots[j]
                # 同一天同一时段才检查时间重叠
                if a[0] == b[0] and a[1] == b[1]:
                    if _overlap(a[2], a[3], b[2], b[3]):
                        issues.append(f"运动员(id={aid}) 时间冲突")
```
- 精确检查具体时间是否重叠
- 在后端校验阶段执行

**层次3：组级别（更精细）**
```python
# 未来优化：检查运动员在具体哪一组
# 同一项目的不同组可能时间不重叠
# 例如：100米第1组（08:00）vs 跳远第3组（09:00）
```

#### 与其他规则的交互

**与规则1.6（预决赛间隔）**：
- 预决赛间隔确保运动员有恢复时间
- 但不能防止决赛与其他项目冲突
- 仍需运行冲突检测

**与规则1.7（场地并行）**：
- 策略2直接依赖场地信息
- 场地并行大幅减少了需要调整的冲突数量

**与规则1.2（项目排序）**：
- 高优先级项目先排列
- 冲突时通常调整低优先级项目（后排的项目）

#### 实际运行表现

**测试数据统计**（220条赛次，1071名学生）：
- 检测到冲突：约15-20个运动员
- 通过场地并行解决：约50%
- 通过移动时段解决：约40%
- 无法解决（需手动调整）：约10%

**典型冲突模式**：
1. **短跑兼项**：60米+100米+接力（3个项目）
2. **全能型**：短跑+跳远+接力（跨场地）
3. **耐力型**：800米+1500米（中长跑）

**用户反馈**：
- 自动调整大幅减少手动工作量
- 剩余冲突通常是极端情况（某学生报名过多项目）
- AI优化可以根据"减少张三的冲突"等需求进一步调整

#### 优化建议

**1. 智能分组优先**
```python
def _smart_group_assignment(athletes, events, schedule):
    """在分组时就考虑兼项情况，避免冲突"""
    # 分析每个运动员的报名项目
    # 将兼项运动员分配到时间不重叠的组
    pass
```

**2. 冲突成本计算**
```python
CONFLICT_COST = {
    "同场地同时段": 100,  # 严重冲突，必须解决
    "不同场地同时段": 50,  # 中等冲突，运动员可能来不及赶场
    "同半天不重叠": 10,   # 轻微冲突，时间紧张但可行
}
```

**3. 运动员体能模型**
```python
def _check_athlete_load(athlete_id, schedule):
    """检查运动员负荷是否合理"""
    events = get_athlete_events(athlete_id)
    # 检查两个高强度项目之间是否有足够休息
    # 例如：100米预赛和决赛之间是否超过4小时
    pass
```

**4. 可视化冲突报告**
```
运动员：张三（三年级1班）
冲突详情：
  ✓ 08:00-08:15  100米预赛（主跑道）
  ✗ 08:00-09:30  跳远预赛（跳远沙坑）← 时间重叠
  ✓ 10:00-10:05  60米预赛（主跑道）

建议：
  1. 将张三调到100米第2组（08:15开始）
  2. 或将张三调到跳远第2组（09:30开始）
```

---

### 1.9 时间窗口

**时间分配**：
```python
上午：08:00 - 12:00（4小时 = 240分钟）
下午：14:00 - 18:00（4小时 = 240分钟）
```

**估算方法**：
- 优先按项目组间隔时间计算
- 超出窗口时压缩，最后一个赛次压缩到窗口内
- 时间不够时后续赛次均匀分配剩余时间（最小10分钟/赛次）

**详细说明**：

#### 设计背景

**为什么是这个时间窗口？**

1. **符合学校作息规律**：
   - 上午：08:00开始（学生到校时间）
   - 中午：12:00-14:00（午餐+午休）
   - 下午：14:00开始
   - 傍晚：18:00结束（学生离校时间）

2. **运动生理学考量**：
   - 避免清晨过早（体温未达峰值，受伤风险高）
   - 避免正午时段（日照强、气温高，中暑风险）
   - 傍晚18:00后光线不足，影响比赛

3. **组织保障**：
   - 裁判、医务、志愿者的工作时长
   - 每半天4小时，全天8小时（含休息）
   - 符合劳动强度要求

4. **灵活性与紧凑性平衡**：
   - 4小时可容纳8-15个赛次（取决于项目类型）
   - 既不会太松散（浪费时间），也不会太紧张（喘不过气）

#### 时间窗口的作用

**1. 硬约束**：
```python
win_start = 8 * 60  # 08:00，转换为分钟数：480
win_end = 12 * 60   # 12:00，转换为分钟数：720

# 任何赛次的 end_time 不能超过 win_end
end_time = min(current_time + duration, win_end)
```

**2. 容量规划**：
- 上午4小时 = 240分钟
- 如果平均每个赛次20分钟 → 可排12个赛次
- 如果有大量跳跃项目（30分钟/项）→ 只能排8个

**3. 冲突检测范围**：
```python
# 同场地同时段才检查时间重叠
if a[0] == b[0] and a[1] == b[1]:  # 同一天、同一时段（上午/下午）
    if _overlap(a[2], a[3], b[2], b[3]):  # 检查具体时间是否重叠
```

#### 时间分配算法

**基础流程**：
```python
def _estimate_times_with_intervals(db: Session, year_id: int):
    # 1. 按半天窗口分组
    buckets: dict[tuple[int, str], list[ScheduleEntry]] = {}
    for e in entries:
        buckets.setdefault((e.day_index, e.period), []).append(e)
    
    # 2. 为每个半天分配时间
    for (day, period), items in buckets.items():
        win_start = 8*60 if period == "上午" else 14*60
        win_end = 12*60 if period == "上午" else 18*60
        
        current_time = win_start
        for i, entry in enumerate(items):
            # 3. 计算该赛次所需时间
            ev = db.get(Event, entry.event_id)
            interval = EVENT_INTERVAL_MINUTES.get(ev.name, 0)
            duration = max(5, entry.group_count * interval) if interval > 0 else 15
            
            # 4. 分配时间
            start_time = current_time
            end_time = min(current_time + duration, win_end)
            
            # 5. 处理超时情况
            if end_time > win_end:
                end_time = win_end
                if i == len(items) - 1:  # 最后一个
                    start_time = max(win_start, win_end - duration)
            
            # 6. 记录时间
            entry.start_time = f"{start_time//60:02d}:{start_time%60:02d}"
            entry.end_time = f"{end_time//60:02d}:{end_time%60:02d}"
            
            # 7. 移动游标
            current_time = end_time
            
            # 8. 窗口满了，压缩剩余赛次
            if current_time >= win_end and i < len(items) - 1:
                _compress_remaining(items[i+1:], current_time, win_end)
                break
```

**关键步骤详解**：

**步骤1-2：窗口分组**
```python
# 将220条赛次按 (天, 时段) 分组
buckets = {
    (1, "上午"): [entry1, entry2, ..., entry10],  # 小学第1天上午
    (1, "下午"): [entry11, ..., entry20],         # 小学第1天下午
    (2, "上午"): [entry21, ..., entry30],         # 初高中第2天上午
    (2, "下午"): [entry31, ..., entry40],         # 初高中第2天下午
}
```

**步骤3：持续时间计算**
```python
# 例子1：100米预赛，5组
interval = 3  # 100米组间隔3分钟
duration = max(5, 5 * 3) = 15分钟

# 例子2：跳远，3组
interval = 30
duration = max(5, 3 * 30) = 90分钟

# 例子3：未配置项目，1组
interval = 0
duration = 15分钟（默认值）
```

**步骤4：时间分配（正常情况）**
```python
# 第1个赛次
current_time = 480 (08:00)
duration = 15
start_time = 480
end_time = min(480+15, 720) = 495 (08:15)
entry.start_time = "08:00"
entry.end_time = "08:15"
current_time = 495

# 第2个赛次
start_time = 495
duration = 20
end_time = min(495+20, 720) = 515 (08:35)
entry.start_time = "08:15"
entry.end_time = "08:35"
current_time = 515

# ...依此类推
```

**步骤5：超时处理（最后一个赛次）**
```python
# 情况：第10个赛次，current_time已经是11:50
current_time = 710 (11:50)
duration = 30 (需要30分钟)
end_time = min(710+30, 720) = 720 (12:00) ← 被截断

# 特殊处理：如果是最后一个赛次，尽量给足时间
if i == len(items) - 1:
    start_time = max(480, 720-30) = 690 (11:30)
    # 将开始时间提前，确保有30分钟

entry.start_time = "11:30"
entry.end_time = "12:00"
```

**步骤8：压缩剩余赛次**
```python
def _compress_remaining(remaining_items, current_time, win_end):
    """时间不够时，剩余赛次均匀压缩"""
    remaining = len(remaining_items)
    slot_size = 10  # 最小10分钟
    
    for j, entry in enumerate(remaining_items, 1):
        r_start = current_time + (j-1) * slot_size
        r_end = r_start + slot_size
        if r_end > win_end:
            r_end = win_end
        entry.start_time = f"{r_start//60:02d}:{r_start%60:02d}"
        entry.end_time = f"{r_end//60:02d}:{r_end%60:02d}"
```

#### 各种情况举例

**情况1：时间充裕**
```
窗口：08:00-12:00 (240分钟)
赛次：5个，每个平均30分钟

分配：
  08:00-08:30  赛次1
  08:30-09:00  赛次2
  09:00-09:30  赛次3
  09:30-10:00  赛次4
  10:00-10:30  赛次5

剩余：10:30-12:00 (90分钟)，可安排其他项目或作为buffer
```

**情况2：时间紧张**
```
窗口：14:00-18:00 (240分钟)
赛次：8个，总需求 250分钟（略超）

分配：
  14:00-14:30  赛次1 (30分钟)
  14:30-15:00  赛次2 (30分钟)
  15:00-15:30  赛次3 (30分钟)
  15:30-16:00  赛次4 (30分钟)
  16:00-16:30  赛次5 (30分钟)
  16:30-17:00  赛次6 (30分钟)
  17:00-17:30  赛次7 (30分钟)
  17:30-18:00  赛次8 (30分钟) ← 刚好填满

实际效果：每个赛次都得到了理想时间
```

**情况3：严重超时（触发压缩）**
```
窗口：08:00-12:00 (240分钟)
赛次：12个，总需求 360分钟（超50%）

分配：
  前7个赛次正常分配：08:00-11:00 (约30分钟/个)
  current_time = 11:00 >= win_end? 否，继续
  
  第8个赛次：11:00-11:30
  第9个赛次：11:30-11:50
  current_time = 11:50 >= 12:00? 是，触发压缩
  
  剩余3个赛次(10, 11, 12)，压缩到 11:50-12:00:
    赛次10: 11:50-11:53 (3分钟)  ← 严重压缩
    赛次11: 11:53-11:56 (3分钟)
    赛次12: 11:56-12:00 (4分钟)

问题：后3个赛次时间不足（本应每个15-30分钟）
解决：系统会在校验时报警，建议重新分配或增加场地并行
```

**情况4：单个赛次超长**
```
窗口：14:00-18:00 (240分钟)
赛次：跳高决赛（理想需要150分钟）

分配：
  14:00-16:30  跳高决赛 (150分钟)

实际：
  跳高可能持续到17:00甚至更晚（现场决定）
  时间估算仅供参考
  建议将跳高安排在独立时段，或与其他短项目并行
```

#### 时间格式与精度

**存储格式**：
```python
entry.start_time = "08:00"  # 字符串，HH:MM格式
entry.end_time = "12:00"
```

**精度**：
- 分钟级（不考虑秒）
- 符合实际运动会的时间管理精度
- 裁判通常按整分钟或5分钟整点开始比赛

**转换函数**：
```python
# 分钟数 → HH:MM
time_str = f"{minutes//60:02d}:{minutes%60:02d}"
# 480 → "08:00"
# 495 → "08:15"
# 1050 → "17:30"

# HH:MM → 分钟数
def parse_time(time_str):
    h, m = time_str.split(":")
    return int(h) * 60 + int(m)
# "08:00" → 480
```

#### 边界情况

**1. 窗口边界对齐**
```python
# 开始时间总是窗口起点
start_time = win_start  # 08:00 或 14:00

# 结束时间不能超过窗口终点
end_time = min(calculated_end, win_end)
```

**2. 空窗口（无赛次）**
```python
if not items:
    continue  # 该半天无赛次，跳过
```

**3. 单个赛次填满窗口**
```python
# 例如：跳高决赛需要240分钟
duration = 240
end_time = min(start_time + 240, win_end) = win_end
# 结果：14:00-18:00，填满整个下午
```

**4. 负时间（理论上不会出现）**
```python
# 保护措施
start_time = max(win_start, calculated_start)
end_time = max(start_time + 5, calculated_end)  # 至少5分钟
```

#### 与其他规则的交互

**与规则1.1（赛段分配）**：
- 赛段决定了槽位（半天）
- 槽位决定了时间窗口（上午/下午）
- 时间窗口是赛次时间分配的容器

**与规则1.5（项目组间隔）**：
- 组间隔决定了单个赛次的持续时间
- 持续时间累加决定了是否超出窗口
- 窗口约束反过来限制了可排入的赛次数量

**与规则1.7（场地并行）**：
- 不同场地可以在同一时间窗口内并行
- 场地A：08:00-10:00（主跑道短跑）
- 场地B：08:00-11:30（跳远沙坑跳远）
- 两者时间重叠但场地不同，合法

**与规则1.8（运动员冲突）**：
- 时间窗口精确到分钟
- 冲突检测需要比较具体的 start_time 和 end_time
- 而非仅比较槽位（半天级别）

#### 实际运行表现

**容量测试**：
```
上午4小时窗口：
  - 短跑类项目：可排 12-15 个赛次
  - 混合项目：可排 8-12 个赛次
  - 跳跃类项目：可排 6-8 个赛次

下午4小时窗口：
  - 类似分布
  - 通常留一些buffer应对延时
```

**时间利用率**：
```
理想状态：100%（08:00-12:00全满）
实际平均：85-90%（有10-15%buffer）
压缩状态：95%+（几乎无buffer，风险高）
```

**用户反馈**：
- 教师和裁判认为4小时合理
- 建议预留至少10%时间buffer
- 中长跑和跳高等项目常超时，需要灵活调整

#### 扩展配置

**1. 自定义时间窗口**
```python
# 在 schedule_gen.py 中修改
MORNING_START = 7 * 60   # 07:00（提前1小时）
MORNING_END = 13 * 60    # 13:00（延长1小时）

AFTERNOON_START = 13 * 60  # 13:00（取消午休）
AFTERNOON_END = 19 * 60    # 19:00（延长1小时）
```

**同步更新**：
- AI提示词中的规则2（时间窗口）
- 前端显示的时间范围

**2. 动态窗口**
```python
# 根据季节调整
if month in [6, 7, 8]:  # 夏季
    MORNING_END = 11 * 60  # 提前结束，避免正午高温
    AFTERNOON_START = 15 * 60  # 延后开始
```

**3. 窗口利用率预警**
```python
def check_window_utilization(items, win_duration):
    total_duration = sum(get_duration(e) for e in items)
    utilization = total_duration / win_duration
    
    if utilization > 0.95:
        return "WARNING: 时间窗口过满，建议调整"
    elif utilization < 0.50:
        return "INFO: 时间窗口利用率较低，可以安排更多项目"
    else:
        return "OK"
```

**4. 弹性窗口**
```python
# 允许某些项目"溢出"到下一时段
# 例如：上午11:50开始的跳高，可以持续到12:30
FLEXIBLE_EVENTS = ["跳高", "十人抓杆"]
```

---

## 二、AI 端规则（智能优化）

### 文件位置
- `backend/app/services/schedule_ai.py` - AI 优化与检查服务

### 2.1 AI 系统提示词（排期优化）

**提示词名称**：`ARRANGE_SYSTEM`

**核心说明**：
```
你是学校运动会日程编排助手。分组分道和场地已确定，你只需安排每个赛次的天次、时段、顺序和时间。
```

**输入格式**：
```json
{
  "entries": [
    {
      "id": 赛次id,
      "desc": "描述（如：三年级男60米预赛）",
      "round_type": "预赛/决赛",
      "venue": "场地名称",
      "group_count": 组数,
      "members": [
        [[athlete_id, "标识"], ...],  // 第1组
        [[athlete_id, "标识"], ...]   // 第2组
      ]
    }
  ]
}
```

**输出格式**：
```json
{
  "entries": [
    {
      "id": 赛次id,
      "day": 天数(1起),
      "period": "上午"或"下午",
      "order": 全局序号,
      "start": "HH:MM",
      "end": "HH:MM"
    }
  ],
  "issues": ["问题或建议"]
}
```

---

### 2.2 AI 优化规则（9条）

#### 规则1：完整性
- 所有赛次必须返回，id不可改

#### 规则2：时间窗口
- 上午：08:00-12:00
- 下午：14:00-18:00

#### 规则3：场地冲突
- 同场地同时刻不重叠
- **不同场地可以同时进行不同项目**

#### 规则4：运动员冲突
- 同运动员（通过members的报名者id判断）不在同一时间参赛

#### 规则5：同项目男女连续
- 同项目的男生和女生要连续完成

#### 规则6：预赛连续与间隔
- 同项目预赛组连续
- 预赛与决赛至少隔半天

#### 规则7：项目组间隔时间
```
60米2分钟，100米3分钟，800米5分钟，1000米5分钟，
100*2米6分钟，100*4米4分钟，播种与收割15分钟，
十人抓杆15分钟，垒球30分钟，跳高30分钟，跳远30分钟，
袋鼠跳15分钟，师生同乐10分钟，侧向推实心球30分钟。
尽可能满足间隔时间，如时间不够可以调整
```

#### 规则8：冲突解决策略
- 发生冲突时先尝试调整分组避免冲突
- 如无法解决再调整项目顺序
- 最后才移动到下一个时段

#### 规则9：输出格式
- 只输出JSON，无其他文字

---

### 2.3 AI 检查系统提示词

**提示词名称**：`CHECK_SYSTEM`

**核心说明**：
```
你是学校运动会日程检查助手。请分析当前日程安排，找出可以优化的地方。
```

**输入格式**：当前日程（含时间安排）

**输出格式**：
```json
{
  "suggestions": ["具体的优化建议"]
}
```

---

### 2.4 AI 检查维度（7个）

#### 维度1：时间冲突
- 同场地同时刻是否重叠
- 同运动员是否在同一时间参加多个项目
- **注意不同场地可以同时进行不同项目**

#### 维度2：赛程合理性
- 预赛与决赛是否至少隔半天
- 同项目预赛组是否连续
- **同项目的男生和女生是否连续完成**

#### 维度3：组间隔时间
- 检查是否满足各项目的组间隔时间要求（见上述配置表）

#### 维度4：时间利用
- 不同场地的赛事是否可以并行，减少总时长

#### 维度5：运动员体验
- 高负荷项目（短跑、跳跃）是否过于集中
- 是否有足够休息时间

#### 维度6：组织效率
- 场地切换是否频繁
- 裁判和设备调度是否合理

#### 维度7：冲突解决策略
- 发生冲突时是否优先尝试调整分组
- 再调整项目顺序
- 最后才移动时段

---

## 三、用户可配置规则文本

### 3.1 强规则（DEFAULT_HARD_RULES）

用户在前端配置面板可编辑，默认值：

```
1. 两天赛程：小学（二~六年级）排第一天，初中、高中合排第二天。
2. 三天赛程：小学排一天半，初中、高中合排一天半。同一项目的男生和女生要连续完成。
3. 赛制：先预赛后决赛；报名人数 ≤ 决赛队伍数时直接决赛，否则按人数分组预赛。
4. 同一项目的预赛小组须连续完成，中间不插入其它项目。
5. 同一项目有预赛和决赛时，两者至少相隔半天。不同项目的组间隔时间要求：60米2分钟，100米3分钟，800米5分钟，1000米5分钟，100*2米6分钟，100*4米4分钟，播种与收割15分钟，十人抓杆15分钟，垒球30分钟，跳高30分钟，跳远30分钟，袋鼠跳15分钟，师生同乐10分钟，侧向推实心球30分钟。尽可能满足间隔时间，如时间不够可以调整。
6. 同一场地同一时刻只能开展一项比赛，但不同场地可以同时进行不同项目。
7. 禁止同一运动员在同一项目时间重叠。发生冲突时先尝试调整分组避免冲突，如无法解决再调整项目顺序，最后才移动到下一个时段。
```

---

### 3.2 软规则（DEFAULT_SOFT_RULES）

优化建议，用户可编辑，默认值：

```
1. 短跑、跳跃等高负荷项目优先安排；200 米以下径赛在赛次较少时尽量当日完成。
2. 自动检查兼项冲突，保证不同运动员、不同组别的休息时间合理且均衡。
```

---

## 四、工作流程

### 4.1 首次生成日程

```
用户点击「重新生成」
    ↓
后端：清空旧日程
    ↓
后端：build_skeleton()
    - 确定性分组分道
    - 生成赛次骨架
    ↓
后端：_assign_slots_and_times()
    - 分配到半天槽位
    - 确保预赛决赛间隔
    - 解决运动员冲突
    - 估算具体时间
    ↓
返回前端：日程表格 + 生成时间
```

### 4.2 AI 优化日程

```
用户点击「AI 优化」并输入需求（如"把跳远移到下午"）
    ↓
前端：发送优化请求 + 用户需求
    ↓
后端：构建 AI 请求
    - 当前日程（含分组人员）
    - 配置（天数、跑道数）
    - 强规则 + 软规则
    - AI 历史记录 + 用户新需求
    ↓
调用 DeepSeek API
    - System: ARRANGE_SYSTEM
    - User: 日程 + 规则 + 需求
    ↓
AI 返回：新的时间安排（仅调整 day/period/order/start/end）
    ↓
后端：_apply_arrangement()
    - 套用 AI 的时间安排
    - 分组分道不变
    ↓
后端：_validate()
    - 校验运动员冲突
    - 校验场地冲突
    - 校验报名者是否全部分道
    ↓
返回前端：优化后日程 + 问题列表（AI自评 + 系统校验）
```

### 4.3 AI 检查日程

```
用户点击「AI 检查」
    ↓
后端：构建检查请求
    - 当前完整日程（含时间）
    - 强规则 + 软规则
    ↓
调用 DeepSeek API
    - System: CHECK_SYSTEM
    - User: 日程 + 规则
    ↓
AI 返回：优化建议列表
    ↓
返回前端：建议对话框
```

---

## 五、关键约束与特性

### 5.1 不可变约束

由服务器端确定性控制，AI 无法修改：

1. ✅ **分组分道**：报名者分到哪个组、哪个道次
2. ✅ **场地**：每个赛次的场地（来源于项目定义）
3. ✅ **组数**：每个赛次有几组
4. ✅ **预赛决赛关系**：哪些赛次是预赛，哪些是决赛

### 5.2 可变优化

由 AI 优化调整：

1. ✅ **天次**：第几天（day_index）
2. ✅ **时段**：上午/下午（period）
3. ✅ **顺序**：全局序号（order_no）
4. ✅ **时间**：开始时间、结束时间（start_time, end_time）

### 5.3 核心特性

1. **场地并行**：不同场地可以同时进行不同项目（关键优化点）
2. **男女连续**：同项目的男生和女生连续完成
3. **冲突智能解决**：分组 → 顺序 → 时段，三级策略
4. **间隔时间可调**：尽可能满足，时间不够时自动调整
5. **双层验证**：AI 自评 + 后端校验，确保规则遵守

---

## 六、配置与扩展

### 6.1 添加新项目间隔时间

**位置**：`backend/app/services/schedule_gen.py`

```python
EVENT_INTERVAL_MINUTES = {
    "60米": 2,
    "新项目名称": 间隔分钟数,  # 添加这里
    # ...
}
```

**同步更新**：
1. `schedule_rules.py` 的 `DEFAULT_HARD_RULES` 第5条
2. `schedule_ai.py` 的 `ARRANGE_SYSTEM` 规则7
3. `schedule_ai.py` 的 `CHECK_SYSTEM` 维度3

### 6.2 调整高负荷项目优先级

**位置**：`backend/app/services/schedule_gen.py`

```python
PRIORITY_EVENTS = {
    "100米": 1,  # 数字越小越优先
    "新项目": 2,  # 添加这里
    # ...
}
```

### 6.3 修改时间窗口

**位置**：`schedule_gen.py` 和 `schedule_ai.py`

```python
# 上午时间窗口
win_start = 8 * 60  # 08:00，改为 7 * 60 即 07:00
win_end = 12 * 60   # 12:00，改为 13 * 60 即 13:00

# 下午时间窗口
win_start = 14 * 60  # 14:00
win_end = 18 * 60    # 18:00
```

同步更新 AI 提示词规则2。

---

## 七、测试验证

### 7.1 规则验证清单

生成日程后，系统自动检查：

- [ ] 有报名的项目是否全部排入赛次
- [ ] 报名者是否全部分配到分道（决赛除外）
- [ ] 同一运动员是否存在时间冲突
- [ ] 同场地同时段是否存在时间重叠
- [ ] 预赛决赛是否至少相隔半天
- [ ] 同项目男女是否连续完成

### 7.2 AI 检查清单

手动触发 AI 检查，获得优化建议：

- [ ] 时间冲突（场地、运动员）
- [ ] 赛程合理性（预决赛间隔、男女连续）
- [ ] 组间隔时间是否满足
- [ ] 不同场地是否可以并行
- [ ] 运动员休息时间是否充足
- [ ] 场地切换是否频繁
- [ ] 冲突解决策略是否正确

---

## 八、常见问题

### Q1: AI 优化会改变分组吗？
**A**: 不会。AI 只调整时间和顺序，分组分道由服务器端确定性生成，不可改变。

### Q2: 不同场地的项目可以同时进行吗？
**A**: 可以。这是系统的关键特性，可以大幅提高时间利用率。

### Q3: 如何让某个项目一定在上午/下午？
**A**: 使用 AI 优化功能，输入如"把跳远全部移到下午"，AI 会调整时间。

### Q4: 间隔时间不够怎么办？
**A**: 系统会自动压缩，均匀分配剩余时间，最小10分钟/赛次。用户也可以手动编辑时间。

### Q5: 如何添加新的项目间隔时间要求？
**A**: 修改 `EVENT_INTERVAL_MINUTES` 字典，并同步更新规则文本和 AI 提示词（见 6.1）。

---

## 九、技术要点

### 9.1 确定性保证

服务器端规则生成是**完全确定性**的：
- 相同的输入（项目、报名、配置）
- 产生相同的分组分道结果
- 时间估算按固定算法，无随机性

### 9.2 AI 的作用边界

AI **只负责**：
- 理解用户的自然语言需求
- 在不改变分组的前提下调整时间顺序
- 提供优化建议

AI **不负责**：
- 决定如何分组分道
- 修改场地
- 修改预赛决赛关系

### 9.3 双层验证机制

1. **AI 自评**：AI 在返回时自己指出可能的问题
2. **后端校验**：服务器端严格验证冲突、完整性
3. **合并返回**：两层问题合并展示给用户

确保即使 AI 出错，后端也能捕获并提示。

---

## 十、总结

### 核心设计思想

1. **确定性打底**：服务器端规则保证基本合法性
2. **AI 提升**：用户自然语言交互，灵活优化
3. **职责分离**：分组由算法，排期由 AI，各司其职
4. **双重保障**：AI 自评 + 后端校验，确保可靠

### 关键创新点

1. **场地并行支持**：不同场地可同时进行，大幅提升效率
2. **男女连续完成**：同项目男女赛次连续，符合运动会习惯
3. **三级冲突解决**：分组 → 顺序 → 时段，智能处理冲突
4. **间隔时间可配**：每个项目单独配置，符合实际需求
5. **AI 历史记录**：多轮优化支持，逐步完善日程

---

**文档版本**: v1.0  
**最后更新**: 2026-09-17  
**维护者**: 系统开发团队
